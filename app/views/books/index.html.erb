<header></header>
<body>
   <% if flash[:notice] %>
     <div class="notice">
       <%= flash[:notice] %>
     </div>
   <% end %>
</p>

<h1>Books</h1>
<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
  </tr>
</table>
<% @books.each do |book| %>
<tbody>
  <tr>
  <td><%= book.title %></td>

  <td><%= book.body %></td>
  <td>
    <td><%= link_to "Show",  book_path(book.id) %></td>
    <td>
    <td><%= link_to "Edit",  edit_book_path(book.id) %></td>
    </td>
    <td>
    <td><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure?" %></td></br>
  </tr>
  </tbody>
  <% end %>
  <h2>New Books</h2>


  <% if @book.errors.any? %>
<div class="error">
  <div class="errors"><%= @book.errors.count %>errors prohibited this book from being saved:</div>

  <div class"errors2">
    <% @book.errors.full_messages.each do |message| %></br>
  <li><%= message %></li>
  <% end %>
  </div>
</div>
<% end %>


  <%= form_with model: @book, local:true do |f| %>
    <div class="aaa">
      <%= f.label :title %>
      <%= f.text_field :title %>
      <%= f.label :body %>
      <%= f.text_area :body %></br>
    </div>
    <%=f.submit 'Create Book'%>
  <% end %>
</body>